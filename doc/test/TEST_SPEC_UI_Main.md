# UI層テスト仕様書 - Main (メイン・利用者一覧)

- **対象テストコード:**
    - `androidTest/java/jp/mydns/fujiwara/carememo/ui/screens/main/MainScreenTest.kt`
    - `test/java/jp/mydns/fujiwara/carememo/viewmodel/PersonListViewModelTest.kt`

## 1. コンポーネント単体テスト (MainScreenContent)
**目的:** 利用者リストの表示、検索、フィルタリング、空状態などの表示ロジックを検証する。

| ID | テスト項目 | 検証内容 |
|:---|:---|:---|
| CP-01 | 空状態の表示 | 利用者が0件の時、空状態のメッセージが表示されること。 |
| CP-02 | ローディング表示 | `isLoading = true` の時、LoadingScreenが表示されること。 |
| CP-03 | 利用者リスト表示 | 利用者が存在する場合、リスト形式で正しく表示されること。 |
| CP-04 | 名前マスキング | 設定が有効な時、氏名が「山○ 太○」のように伏せ字で表示されること。 |
| CP-05 | 記録バッジの表示 | 各カテゴリ（血圧・所見・服薬）に記録がある場合、対応するバッジが表示されること。 |
| CP-06 | 検索結果なし表示 | 検索ワードに一致する利用者がいない時、専用のメッセージが表示されること。 |

## 2. 画面全体の挙動・結合テスト (MainScreen)
**目的:** ユーザーの操作（検索、メニュー、ナビゲーション）が正しく機能することを検証する。

| ID | テスト項目 | 検証内容 |
|:---|:---|:---|
| BH-01 | ハンバーガーメニュー | メニューボタン押下でドロワーが開き、設定やバージョン情報へ遷移できること。 |
| BH-02 | 検索機能 | 検索窓へのテキスト入力で、`onSearchQueryChange` が呼ばれること。 |
| BH-03 | カナ索引の選択 | カナ索引（あ・か・さ...）のタップで、`onSectionSelect` が呼ばれること。 |
| BH-04 | 利用者メニュー | 各項目の編集ボタン押下で、編集ダイアログや利用終了の操作ができること。 |
| BH-05 | 利用終了とUndo | 利用終了時、スナックバーが表示され「元に戻す」で操作がキャンセルできること。 |
| BH-06 | 新規追加ボタン | 右下の「＋」ボタン押下で、`onAddClick` コールバックが呼ばれること。 |

## 3. ロジック・安全性テスト (PersonListViewModel)
**目的:** 例外発生時もローディング状態が適切に解除され、監査ログにエラーが記録されることを検証する。

| ID | テスト項目 | 検証内容 |
|:---|:---|:---|
| LG-01 | データ取得失敗時の安全性 | 利用者一覧の取得（Flow）中に例外が発生した際、`isLoading` が `false` になり、エラーダイアログが表示され、監査ログに "ERROR" が記録されること。 |
| LG-02 | 保存/削除失敗時の安全性 | 利用者の登録、利用終了、復帰処理中に例外が発生した際、`isLoading` が `false` になり、エラーダイアログが表示され、監査ログに "ERROR" が記録されること。 |

## 4. テスト用タグ (testTag) 定義
- `MainScreen_EmptyState`: 空状態表示
- `MainScreen_Loading`: ローディング画面
- `MainScreen_UserList`: 利用者リスト
- `UserListItem_{id}`: 各利用者の項目
- `MainScreen_SearchBox`: 検索窓
- `MainScreen_MenuButton`: ハンバーガーメニューボタン
- `MainScreen_AddButton`: 新規登録ボタン
- `UserListItem_MenuButton`: 各項目の操作メニューボタン

## 4. 実装状況
今回のUIテストおよびロジックテストの実装において、仕様書に定義されたすべての項目が実装され、検証されている。

| セクション | 項目 ID | ステータス | 備考 |
|:---|:---|:---:|:---|
| 1. コンポーネント単体 | CP-01 〜 CP-06 | ✅ 実装済み | 表示ロジック、マスキング、バッジ表示の網羅。 |
| 2. 画面挙動・結合 | BH-01 〜 BH-06 | ✅ 実装済み | ナビゲーション、検索、利用者メニュー操作の網羅。 |
| 3. ロジック・安全性 | LG-01 〜 LG-02 | ✅ 実装済み | 例外発生時のローディング解除と監査ログ記録。 |
